# Chapter 12 - Protein Transformers (Python 3.12)
# Lean, consistent requirements regenerated from pyproject.toml.
#
# The previous file was a full transitive environment freeze that pinned an
# inconsistent torch/torchvision/torchaudio/triton set (torch 2.8.0 with
# torchvision 0.17.2 / torchaudio 2.2.2 / triton 2.2.0) which cannot install
# together. torchvision/torchaudio are not imported by any Ch12 code and were
# removed. For the package plus a chosen extra instead, use:
#     pip install -e ".[notebooks]"     (or  uv pip install -e ".[notebooks]")
#
# SECURITY: all 82 Dependabot alerts on this repo were raised against the old
# frozen pin-list in THIS file. The floors below are set at-or-above the first
# patched version for every advisory that has one, so the declared ranges no
# longer admit a vulnerable release (ray CVE was critical; pyarrow/jupyterlab
# high; torch medium). See qa_reports/SECURITY.md.

# --- Core ---
torch>=2.10.0
numpy>=1.26.4
pandas>=2.2.0
scikit-learn>=1.6.0
matplotlib>=3.10.0
seaborn>=0.13.2
typer>=0.12.0
pyarrow>=23.0.1
typing-extensions>=4.0.0

# --- Notebooks (ESM-2 via Hugging Face, BioPython sequences, viz/widgets) ---
transformers>=4.44.0
biopython>=1.83
tqdm>=4.67.1
ipywidgets>=8.1.0
cmocean>=4.0.3
jupyterlab>=4.5.9
ipykernel>=6.29.0
jupytext>=1.16.0

# --- Hyperparameter tuning (scripts/tune.py) — optional ---
ray[tune]>=2.54.0
